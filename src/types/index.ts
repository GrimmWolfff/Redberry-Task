export type Laptop = { 
    image: string, 
    name: string,
    id: number
};

export type CPU = {
    id: number,
    name: string
};

export type User = { 
    name: string,
    surname: string,
    team_id: number,
    position_id: number,
    email: string,
    phone_number: string
};
